usando a biblioteca openpyxl porém também podemos usar a mesma para editar uma planilha já criada no modo adm

import openpyxl

book = openpyxl.load_workbook('Banco de dados.xlsx')

quiz_page = book['Quiz']

for rows in quiz_page.iter_rows(min_row=2,max_row=6):
   for cell in rows:
       if cell.value == '1':
           cell.value = '1º'
       if cell.value == '2':
            cell.value = '2º'
       if cell.value == '3':
           cell.value = '3º'
       if cell.value == '4':
           cell.value = '4º'
       if cell.value == '5':
           cell.value = '5º'
book.save('Banco de dados v2.xlsx')
